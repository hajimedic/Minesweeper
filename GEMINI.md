# 大前提
日本語で回答すること

# プロジェクト概要
このプロジェクトは、Python と Pygame を使用したマインスイーパーの実装です。
コードの保守性を高めるため、ゲームロジックと UI 描画が分離されています。

# 開発ガイドライン
- **関心の分離**: 
    - `minesweeper.py` には純粋なゲームロジックのみを記述し、Pygame への依存を含めないこと。
    - `game_ui.py` でユーザーインターフェースとイベント入力を管理すること。
- **コードスタイル**: 
    - PEP 8 に準拠した命名規則（snake_case）を使用すること。
    - 関数やクラスには適切な docstring を追加すること。
- **テスト**: 
    - テスト駆動開発（TDD）のアプローチを採用すること。ロジックの修正や機能追加を行う際は、先に `tests/` ディレクトリ内のユニットテストを記述・更新し、その後実装を行うこと。
    - 実装後は `pytest` によるテストが全てパスすることを確認すること。
- **コミットメッセージ**: 
    - 変更内容が明確にわかるように、日本語で簡潔に記述すること。
- **PRレビューの確認手順**: 
    - PRにレビューコメントがついた際は、AIアシスタント自身が GitHub CLI (`gh`) コマンドを用いて内容を自律的に取得・対応すること。
    - 例1 (全体コメントの確認): `gh pr view {pr_number} --comments`
    - 例2 (コードごとの具体的な指摘の確認): `gh api repos/{owner}/{repo}/pulls/{pr_number}/comments | jq -r '.[] | "File: \(.path)\nLine: \(.line)\nComment: \(.body)\n---"'`
